# 📘 README.md

## Multi-Inheritance แบบ Java (Interfaces + Composition)

---

## 1. Java สืบทอดหลาย class ไม่ได้เพราะอะไร

ภาษา Java **ไม่อนุญาตให้ class สืบทอดหลาย class พร้อมกัน**
เนื่องจากอาจทำให้เกิดปัญหาที่เรียกว่า **Diamond Problem**

Diamond Problem คือกรณีที่ class ลูกได้รับเมธอดชื่อเดียวกันจาก class แม่หลายตัว
เช่น หาก class A และ class B มีเมธอดชื่อเดียวกัน แล้ว class C สืบทอดทั้งสอง
Java จะไม่สามารถตัดสินใจได้ว่าควรใช้เมธอดของ class ใด ทำให้เกิดความกำกวม (ambiguity)

เพื่อป้องกันปัญหานี้ Java จึงจำกัดให้ `extends` ได้เพียง **1 class เท่านั้น**

---

## 2. เราใช้ interface + composition แทนอย่างไร

แทนที่จะสืบทอดหลาย class เราใช้แนวคิดดังนี้

### 2.1 Multiple Interfaces

* ใช้ `interface` เพื่อกำหนด **บทบาท (role)** หรือ **พฤติกรรม**
* class หนึ่งสามารถ `implements` interface ได้หลายตัว
* interface ไม่เก็บ state จึงไม่เกิดปัญหาแบบ class

ในงานนี้ `RescueRobot` implements:

* Flyable
* Drivable
* ThermalSensor
* Communicable

เพื่อแสดงว่าหุ่นยนต์สามารถทำหลายบทบาทพร้อมกันได้

### 2.2 Composition (has-a relationship)

* แยกความสามารถจริงออกเป็นคลาสย่อย (component)
* RescueRobot “มี” (has-a) องค์ประกอบเหล่านี้ ไม่ได้สืบทอดจากมัน

ได้แก่:

* Battery → จัดการพลังงาน
* GPSTracker → จัดการตำแหน่ง
* ThermalCamera → ตรวจจับอุณหภูมิ

แนวคิดนี้เรียกว่า **Composition over Inheritance**
ช่วยให้โค้ดมีความยืดหยุ่น แก้ไขง่าย และใกล้เคียงการออกแบบระบบจริง

---

## 3. การแก้ปัญหา default method ชื่อชนกัน

ในโจทย์นี้:

* Flyable มี `default String status()`
* Drivable มี `default String status()`

เมื่อ `RescueRobot` implements ทั้งสอง interface
จะเกิด **compile-time error** เพราะ Java ไม่รู้ว่าจะใช้ `status()` ของ interface ใด

### วิธีแก้

* ต้อง override เมธอด `status()` ใน `RescueRobot` เอง
* และเรียก default method ของแต่ละ interface แบบเจาะจงด้วย `InterfaceName.super.method()`

ตัวอย่างแนวคิด:

* ใช้ `Flyable.super.status()`
* ใช้ `Drivable.super.status()`
* รวมผลลัพธ์ให้ชัดเจนในเมธอดเดียว
